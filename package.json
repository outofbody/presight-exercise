{
  "name": "presight-execise",
  "version": "1.0.0",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/outofbody/presight-exercise.git"
  },
  "author": "outofbody",
  "license": "MIT",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "start": "lerna run start",
    "dev": "lerna run dev --parallel",
    "build": "lerna run build",
    "test": "lerna run test",
    "test:server": "yarn workspace presight-server test",
    "test:client": "yarn workspace presight-client test",
    "lint": "lerna run lint",
    "lint:fix": "lerna run lint:fix",
    "validate": "lerna run validate",
    "validate:server": "yarn workspace presight-server validate",
    "validate:client": "yarn workspace presight-client validate"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "lerna": "~8.2.1"
  }
}
